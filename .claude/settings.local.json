{
  "permissions": {
    "allow": [
      "Bash(where gcloud)",
      "Bash(gcloud config list)",
      "Bash(gcloud builds submit --tag us-central1-docker.pkg.dev/podcast-finder-488414/api/podcast-finder:latest)",
      "Bash(gcloud run deploy podcast-finder --image us-central1-docker.pkg.dev/podcast-finder-488414/api/podcast-finder:latest --region us-central1 --platform managed --allow-unauthenticated --set-env-vars FAISS_GCS_URI=gs://podcast-finder-data/nerdcast.index,FAISS_MAPPING_GCS_URI=gs://podcast-finder-data/embedding_id_mapping.npy,FAISS_DB_GCS_URI=gs://podcast-finder-data/nerdcasts.db --memory=2Gi --cpu=1 --concurrency=1 --timeout=1000s)",
      "Bash(Start-Sleep -Seconds 30)",
      "Bash(gcloud run services logs read podcast-finder --region us-central1 --limit 100)",
      "Bash(gcloud run services logs read podcast-finder --region us-central1 --limit 80)",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" --max-time 60 \"https://podcast-finder-956567384706.us-central1.run.app/health\")"
    ]
  }
}
